# Contribution Guide

1. Fork the repository and create a branch: `git checkout -b feat/my-feature`
2. Follow the naming conventions (Angular/Conventional Commits)
3. Run the tests locally.
4. Open a PR to `main`. The CI must pass.

5. At least one review is required before merging.

For any questions, use `GitHub Discussions` or open a `question` issue.
